# seeing-eye-scorer

## Fine-tuning image intake

A YOLO-compatible dataset scaffold for new fine-tuning images lives in
`datasets/fine_tuning`.
See `datasets/fine_tuning/README.md`
for the intake and annotation workflow.
